# Waiver Register

## Active waivers

None.

## Notes

- `dataVisualization/interactiveTree` remains marked `partial` in whitelist metadata, but this is represented as governed metadata with documented gaps rather than an active release waiver.
- No temporary bypass was introduced for tests, compatibility gates, or permission semantics during this implementation session.
