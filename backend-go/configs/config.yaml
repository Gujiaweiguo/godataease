server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 3306
  name: dataease10
  user: root
  password: ""
  max_open_conns: 100
  max_idle_conns: 10

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  pool_size: 100

jwt:
  secret: "your-secret-key-change-in-production"
  expire: 86400

log:
  level: info
  format: console

telemetry:
  enabled: false
  endpoint: ""
