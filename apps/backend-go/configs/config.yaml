server:
  port: 8100
  mode: debug

database:
  host: mysql8
  port: 3306
  name: dataease_dev
  user: root
  password: "Admin168"
  max_open_conns: 100
  max_idle_conns: 10

redis:
  host: redis-dataease
  port: 6379
  password: ""
  db: 0
  pool_size: 100

jwt:
  secret: "your-secret-key-change-in-production"
  expire: 86400

log:
  level: info
  format: console

telemetry:
  enabled: false
  endpoint: ""
